# NutriMind AI — Environment Variables
# Copy this file to .env and adjust for your local setup.
# NEVER commit the real .env file to version control.

APP_ENV=development

# PostgreSQL
POSTGRES_DB=nutrimind
POSTGRES_USER=nutrimind
POSTGRES_PASSWORD=change_this_in_real_environments
POSTGRES_PORT=5432
